/**
 * Create an API reference for the raster context
 * @returns {Object} The high-level raster API
 */
export default function getAPI() {
    const context = this;
    const state = {};

    return {
        test: () => console.debug("Not yet implemented")
    };
}